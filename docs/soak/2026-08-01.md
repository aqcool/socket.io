# 2026-08-01 集群 3 小时稳定性验收记录

本记录对应项目对标官方 Socket.IO 的独立稳定性门禁。它验证长连接、持续 ACK/广播、主动重连、网络分区、滚动替换以及资源回收，不计入官方源测试行的 1004 项分母。

## 环境与命令

- 日期：2026-08-01（Asia/Shanghai）
- 平台：macOS / amd64
- Go：1.26.0
- Node.js：22.23.1
- npm：10.9.8
- 官方客户端依赖：由 `adapters/unix/testdata/cluster-official/package-lock.json` 锁定并通过 `npm ci` 安装

```bash
cd adapters/unix
SOCKET_IO_CLUSTER_SOAK=1 \
SOCKET_IO_SOAK_DURATION=3h \
SOCKET_IO_SOAK_PARTITION_DURATION=20m \
SOCKET_IO_SOAK_CLIENTS=72 \
  go test -mod=mod ./adapter \
    -run '^TestOfficialClusterSoakAndResourceLeaks$' \
    -v -count=1 -timeout=3h30m
```

## 结果

结果：**通过**，测试进程退出码为 0，测试耗时 10805.92 秒。

| 指标 | 结果 |
| --- | ---: |
| 官方客户端 | 72 |
| 建立连接次数 | 1675 |
| 断开次数 | 1603 |
| ACK 成功/尝试 | 388307 / 388308 |
| ACK 超时 | 1 |
| 广播交付率 | 1.0000 |
| 网络分区受影响客户端 | 25 |
| 滚动替换受影响客户端 | 24 |
| 聚合集群资源样本 | 36 |
| 可比负载客户端数 | 72 / 72 |

网络分区与滚动替换两个故障阶段的恢复状态均为 `passed`。四个工作进程分别采集 36、14、22、36 份周期资源样本；测试结束后客户端、goroutine、堆内存、堆对象、栈内存和文件描述符均通过既定回落门槛，未触发资源趋势或残留资源断言。

包含主动故障阶段在内的整轮验收仅记录一次 ACK 超时，最终 ACK 成功率约为 99.9997%，高于长时验收阈值；广播交付无丢失。完整门禁仍由 [工作流](../../.github/workflows/soak.yml) 每周及按需重复执行。
